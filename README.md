# T9 Emulator

This project aims to emulate t9 texting. It will be implemented within a unix
terminal. At its core, it must support texting with only 0-9, \*, and # keys.
Later iterations should also include autocorrect (i.e. include word suggestions
whose Levenshtein distance >0)
